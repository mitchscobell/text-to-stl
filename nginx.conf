server {
    listen 3000;
    server_name localhost;
    
    root /usr/share/nginx/html;
    index index.html;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/json application/xml+rss;

    # Hashed webpack bundles only. Do not mark HTML/SVG/unhashed JS immutable
    # or Cloudflare will keep serving a stale /index.js for a year.
    location ~* \.[0-9a-f]{8}\.js$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location ~* \.(woff2?|ttf|otf|eot)$ {
        expires 30d;
        add_header Cache-Control "public";
    }

    # Version endpoint - JSON API for health checks
    location = /version.json {
        add_header Content-Type application/json;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # Version page - HTML page with version info
    location = /version {
        rewrite ^/version$ /version.html break;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # Main location - serve index.html for all routes (SPA routing)
    location / {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files $uri $uri/ /index.html;
    }

    # Error pages
    error_page 404 /index.html;
}
